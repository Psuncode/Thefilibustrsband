---
import { homepageContent } from "../../data/homepage";

const feedbackRows = [
  homepageContent.personality.feedback.filter((_, index) => index % 2 === 0),
  homepageContent.personality.feedback.filter((_, index) => index % 2 === 1)
];
---

<section class="bg-[var(--color-ink)] text-white">
  <div class="mx-auto max-w-6xl px-4 py-12 md:px-6 md:py-16">
    <div class="flex flex-col gap-3">
      <p class="text-sm font-bold uppercase tracking-[0.2em] text-[var(--color-pink)]">Proof of life</p>
      <h2 class="display-face max-w-4xl text-3xl uppercase leading-[0.95] tracking-[-0.03em] md:text-5xl">
        {homepageContent.personality.feedbackTitle}
      </h2>
      <p class="max-w-2xl text-base leading-7 text-white/68">
        We could try to explain it, but this says it better.
      </p>
    </div>
    <div class="mt-8 flex flex-col gap-4 overflow-hidden md:mt-10 md:gap-5">
      {feedbackRows.map((row, rowIndex) => (
        <div class="feedback-marquee-mask">
          <div class:list={["feedback-track", rowIndex % 2 === 1 && "feedback-track-reverse"]}>
            {row.map((item, index) => (
              <blockquote
                class:list={[
                  "feedback-card",
                  index % 3 === 1
                    ? "border-[var(--color-yellow)]/45 bg-[var(--color-yellow)]/10"
                    : "border-white/15 bg-white/5"
                ]}
              >
                <p class="text-base leading-7 text-white/90 md:text-[1.0625rem] md:leading-8">"{item.quote}"</p>
                <footer class="mt-5 text-xs font-bold uppercase tracking-[0.18em] text-[var(--color-yellow)]">
                  {item.attribution}
                </footer>
              </blockquote>
            ))}
            {row.map((item, index) => (
              <blockquote
                aria-hidden="true"
                class:list={[
                  "feedback-card feedback-card-duplicate",
                  index % 3 === 1
                    ? "border-[var(--color-yellow)]/45 bg-[var(--color-yellow)]/10"
                    : "border-white/15 bg-white/5"
                ]}
              >
                <p class="text-base leading-7 text-white/90 md:text-[1.0625rem] md:leading-8">"{item.quote}"</p>
                <footer class="mt-5 text-xs font-bold uppercase tracking-[0.18em] text-[var(--color-yellow)]">
                  {item.attribution}
                </footer>
              </blockquote>
            ))}
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
