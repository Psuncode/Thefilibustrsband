# Work Log

Use this file for short milestone-style updates that future agents may need for context.

Guidelines:

- Keep entries brief and factual.
- Log milestones, not full conversations.
- Prefer one entry per meaningful repo change or decision cluster.
- If an entry stops being useful, summarize or delete it.

## 2026-03-30

- Added Vercel Analytics dependency and mounted analytics in the shared base layout.
- Replaced the favicon with a megaphone-style SVG at `public/favicon.svg`.
- Rewrote `AGENTS.md` to function as cross-tool startup memory instead of a generic contributor guide.
- Added `docs/memory/` as the lightweight durable-memory layer for this repo.
